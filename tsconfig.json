{
  "compilerOptions": {
    "target": "ESNext",         // Target modern JavaScript for ESNext features.
    "module": "ESNext",         // Use ESNext module system.
    "jsx": "react-jsx",         // Use the JSX transformation for React (React 17+).
    "strict": true,             // Enable strict type-checking options.
    "esModuleInterop": true,    // Enable compatibility with CommonJS modules.
    "allowSyntheticDefaultImports": true, // Allow default imports from modules with no default export.
    "skipLibCheck": true,       // Skip checking types in libraries for performance.
    "forceConsistentCasingInFileNames": true, // Enforce case sensitivity in imports.
    "moduleResolution": "node", // Use Node.js module resolution strategy.
    "types": ["react-dom/experimental", "node"], // Include React types.
  },
  "include": [
    "app/javascript/**/*", // Include your React/JavaScript files.
    "app/src/**/*"     // Include any other front-end TypeScript files.
, "app/src/types", "app/src/components"  ],
  "exclude": [
    "node_modules",         // Exclude node_modules folder to speed up the build process.
    "tmp",                  // Exclude temporary files from Rails.
    "public"                // Exclude the public folder.
  ]
}